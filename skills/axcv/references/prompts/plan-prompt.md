# Plan Prompt 模板

根据结构化规范，输出实施计划：

1. 涉及文件列表（包括新增、修改、删除的文件）
2. 实施步骤（按依赖关系排序）
3. 测试变更（单元测试、集成测试、E2E 测试的修改）
4. 回滚备注（如果某步骤失败，如何回滚）

## 输出要求

- 格式：Markdown
- 保存路径：`docs/执行计划/[任务名]-plan.md`
- 必须落盘，不能只存在于对话中
- 计划制定完成后，无需用户确认，立即进入执行阶段
